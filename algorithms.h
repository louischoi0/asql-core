#include <cstring>
#include "my-redis.h"
#include <stdio.h>

int printArr(uint* arr,uint size);
int swap(uint*,uint*);
int find(uint f, uint* arr, uint size);
int quick(uint*,uint*,uint);
int testSort();


